"""Settings shim for arches-${name}.

Only defines app-specific overrides. The host project's settings must do
``from arches_${name}.settings import *`` if it wants them applied.
"""

APP_NAME = "arches_${name}"
