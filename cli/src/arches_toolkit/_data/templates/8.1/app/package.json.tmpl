{
  "name": "arches-${package_dashed}",
  "version": "0.0.1",
  "description": "Frontend dependencies for the arches-${package_dashed} Arches application. Consumers reference this app as an npm git dependency (git+<repo>#<ref>) so these install transitively — no lifecycle scripts: npm runs `prepare` when installing git deps.",
  "license": "AGPL-3.0-only",
  "dependencies": {}
}
